// @deprecated — peça do design artem-style anterior da rota /eu.
// Substituído pelo `components/bento-eu.tsx` (bento grid no design system da home).
// Mantido apenas como stub pra não quebrar imports legados.
export default function ArrowListItem() {
  return null;
}
